{
  "name": "@ai-switch/plugin-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A minimal, permission-free APLG text statistics plugin example.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ai-switch/plugin-example.git"
  },
  "packageManager": "pnpm@10.12.4",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "test": "node --test tests/*.test.mjs",
    "plugin:validate": "aplg validate --stage dist",
    "plugin:pack": "aplg pack"
  },
  "dependencies": {
    "@ai-switch/tauri-plugin-runtime": "0.1.0"
  },
  "devDependencies": {
    "@ai-switch/tauri-plugin-devkit": "0.1.0",
    "vite": "8.3.0"
  }
}
